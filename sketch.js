x = 400
y = 400
speed = 1

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
  rect(width/2, height/2, 50, 50)
  if(x >= 800 && y >= 800){
    x = 400
    y = 400)
  }
}
